streamlit>=1.35.0
pandas>=2.0.0
plotly>=5.20.0
requests>=2.31.0
python-dotenv>=1.0.0
fpdf2>=2.7.0
# Fijado: MotherDuck aún no soporta duckdb 1.5.4 (máx 1.5.3). NO subir sin
# confirmar que MotherDuck ya soporta la nueva versión, o se rompe la conexión.
duckdb==1.5.3

# numpy < 2 OBLIGATORIO: duckdb 1.5.3 (y otras wheels nativas) se compilaron
# contra numpy 1.x. Con numpy 2.x el ABI rompe y Streamlit Cloud crashea al
# arrancar con "Segmentation fault" (duckdb se usa apenas carga el warehouse).
# No subir a numpy 2 sin verificar que TODA la cadena binaria lo soporte.
numpy>=1.24.0,<2

# Realimentación / entrenamiento del scorer (liviano, corre en la nube y local).
scikit-learn>=1.3.0
joblib>=1.3.0

# ── Analizador de viralidad: las dependencias MULTIMEDIA pesadas (opencv,
# librosa, soundfile, faster-whisper/ctranslate2) NO van acá: se movieron a
# requirements-analyzer.txt para uso LOCAL. En Streamlit Cloud causaban
# Segmentation fault y además no pueden correr sin ffmpeg/Ollama instalados.
